const fs = require('fs'); const fetch = require('node-fetch'); async function upload() { const formData = new (require('formdata-node').FormData)(); formData.append('file', new (require('formdata-node').File)([fs.readFileSync('test.txt')], 'test.txt', {type: 'text/plain'})); formData.append('upload_preset', 'UniTravel'); const res = await fetch('https://api.cloudinary.com/v1_1/dlsf0ikpc/image/upload', {method: 'POST', body: formData}); console.log(await res.text()); } upload();
