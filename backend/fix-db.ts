import mongoose from 'mongoose'; import databaseService from './services/database.services'; async function run() { await databaseService.connect(); const db = mongoose.connection.db; const experiences = db.collection('experiences'); await experiences.updateMany({ meetingPoint: { $exists: false } }, { $set: { meetingPoint: { type: 'Point', coordinates: [108.2022, 16.0544] } } }); console.log('Fixed DB!'); process.exit(0); } run();
