import { v2 as cloudinary } from 'cloudinary'; import { config } from 'dotenv'; import axios from 'axios'; config(); cloudinary.config({ cloud_name: process.env.CLOUDINARY_CLOUD_NAME, api_key: process.env.CLOUDINARY_API_KEY, api_secret: process.env.CLOUDINARY_API_SECRET }); async function uploadBuffer(url) { const response = await axios.get(url, { responseType: 'arraybuffer' }); const buffer = Buffer.from(response.data, 'binary'); return new Promise((resolve, reject) => { cloudinary.uploader.upload_stream({ folder: 'unitravel_seed' }, (error, result) => { if (error) return reject(error); resolve(result.secure_url); }).end(buffer); }); } async function run() { try { const url1 = await uploadBuffer('https://images.unsplash.com/photo-1582878826629-29b7ad1cb431'); console.log('Food:', url1); const url2 = await uploadBuffer('https://images.unsplash.com/photo-1583417646197-09d2110c7324'); console.log('Bridge:', url2); } catch (e) { console.error(e); } } run();
